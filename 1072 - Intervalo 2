int main()
{
    int input, contador1= 0,contador2 =0;
    long long int valor;
    scanf("%d",&input);
    
    for(int i = 0;input > i; i++){
        scanf("%lld",&valor);
        if (valor >= 10 && valor <= 20){
            contador1 ++;
        }else{
            contador2 ++;
        }
    }
    printf("%d in\n",contador1);
    printf("%d out\n",contador2);

    return 0;
}
