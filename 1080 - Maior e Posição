#include <stdio.h>

int main(void){
    int i, maior = -1, posicao, n;

    for(i = 1; i <= 100; i++){
        scanf("%d", &n);
        if (n > maior){
            maior = n;
            posicao = i;
        }
    }

    printf("%d\n%d\n", maior, posicao);

    return 0;
}

